import { Typography } from '@mui/material';
import { AdminLayout } from '../../layout/AdminLayout';

export const Pantallas = () => {
  return (
    <AdminLayout>
      <Typography variant="h3">Pantallas</Typography>
    </AdminLayout>
  );
};